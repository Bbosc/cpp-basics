// 3. Write a in C++ program to find the size of fundamental data types.
#include <iostream> 


int main (int argc, char *argv[]) {

  std::cout << "the sizeof a char is: " << sizeof(char) << '\n';
  std::cout << "the sizeof a int is: " << sizeof(int) << '\n';
  // and so on ...
  
  return 0;
}
